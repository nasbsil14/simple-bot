package bot.typetalk.io.json

final case class TypetalkWebhookResponse(
                                          message: String,
                                          replayTo: String
                                        )
